SCHEDULE OPTIMIZATION MODULE

Useful functions:
1. input week, get best schedules
- needs some metric of how good a schedule is
- display different amount of teams depending on their scores being above some threshold compared to average
    - games + off nights
- default to current week

2. input roster, position slots, get list of days + positions that are available
- needs some kind of algorithm that takes into account dual/triple eligible players as well as UTIL slots
- places all single eligible players in their slots first
- matching algorithm

3. input roster, position, position slots, get list of which days are available
- still similar algorithm to above, but any dual eligible player that has the input position instantly gets that positionality removed


Current challenges:
- need some kind of players database for all functions to access, with team data so that this module can take players rather than teams
